# Base URL of your Gitea instance, without /api/v1.
GITEA_URL=https://git.example.com

# Create a Gitea access token with only the permissions the connector needs.
# Never commit a real token.
GITEA_TOKEN=
